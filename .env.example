# Copy this to .env and fill in values — never commit .env to git
BASE_URL=https://staging.peptiderepo.com
WP_ADMIN_USER=peptiderepo
WP_ADMIN_PASSWORD=your-staging-password-here

# Page paths where shortcodes live (adjust to match actual WP page slugs)
CALC_PAGE_PATH=/reconstitution-calculator/
SEARCH_PAGE_PATH=/search-peptides/
DIRECTORY_PAGE_PATH=/peptide-directory/
