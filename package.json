{
  "name": "peptide-e2e",
  "version": "1.0.0",
  "description": "Playwright smoke tests for peptiderepo.com (staging + production)",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:staging": "BASE_URL=https://staging.peptiderepo.com playwright test",
    "test:prod": "BASE_URL=https://peptiderepo.com playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.43.0",
    "dotenv": "^16.4.5"
  }
}
